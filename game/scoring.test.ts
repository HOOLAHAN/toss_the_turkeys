import { describe,expect,it } from 'vitest'; import { calculateTossScore } from './scoring'; import { TurkeyPosition } from '../types/turkey';
const cases:[TurkeyPosition,TurkeyPosition,number][]=[['prone_poultry','prone_poultry',1],['prone_poultry','beaky_one',0],['prone_poultry','gobbler',5],['prone_poultry','turkey_trot',5],['prone_poultry','jiblets',10],['prone_poultry','wattle_wobble',15],['beaky_one','beaky_one',1],['beaky_one','gobbler',5],['beaky_one','turkey_trot',5],['beaky_one','jiblets',10],['beaky_one','wattle_wobble',15],['gobbler','gobbler',20],['gobbler','turkey_trot',10],['gobbler','jiblets',15],['gobbler','wattle_wobble',20],['turkey_trot','turkey_trot',20],['turkey_trot','jiblets',15],['turkey_trot','wattle_wobble',20],['jiblets','jiblets',40],['jiblets','wattle_wobble',25],['wattle_wobble','wattle_wobble',60]];
describe('all 21 unordered combinations',()=>{it.each(cases)('%s + %s = %i',(a,b,points)=>expect(calculateTossScore(a,b).points).toBe(points))});
it('plucks in either order',()=>{expect(calculateTossScore('prone_poultry','beaky_one').isPlucked).toBe(true);expect(calculateTossScore('beaky_one','prone_poultry').isPlucked).toBe(true)});
it('marks Thanksgiving',()=>expect(calculateTossScore('wattle_wobble','wattle_wobble')).toMatchObject({points:60,isThanksgiving:true,isDouble:true}));
