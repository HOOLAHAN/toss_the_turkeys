import AsyncStorage from '@react-native-async-storage/async-storage'; import { create } from 'zustand'; import { createJSONStorage,persist } from 'zustand/middleware'; import { LifetimeStatistics } from '@/types/game'; import { TossScoreResult } from '@/types/turkey';
const empty:LifetimeStatistics={gamesPlayed:0,totalTosses:0,totalPointsScored:0,pluckedCount:0,pronePoultryCount:0,beakyOneCount:0,gobblerCount:0,turkeyTrotCount:0,jibletsCount:0,wattleWobbleCount:0,doubleGobblerCount:0,doubleTurkeyTrotCount:0,doubleJibletsCount:0,doubleWattleWobbleCount:0,highestTurnScore:0,highestSingleTossScore:0};
const positionKey={prone_poultry:'pronePoultryCount',beaky_one:'beakyOneCount',gobbler:'gobblerCount',turkey_trot:'turkeyTrotCount',jiblets:'jibletsCount',wattle_wobble:'wattleWobbleCount'} as const;
interface StatsState extends LifetimeStatistics { recordToss:(r:TossScoreResult)=>void; recordGame:(bestTurn:number)=>void }
export const useStatisticsStore=create<StatsState>()(persist((set)=>({...empty,recordToss:r=>set(s=>{const a=positionKey[r.turkeyA],b=positionKey[r.turkeyB]; const next={...s,totalTosses:s.totalTosses+1,totalPointsScored:s.totalPointsScored+r.points,pluckedCount:s.pluckedCount+(r.isPlucked?1:0),highestSingleTossScore:Math.max(s.highestSingleTossScore,r.points),[a]:s[a]+1}; next[b]=next[b]+1; if(r.isDouble&&r.turkeyA==='gobbler')next.doubleGobblerCount++; if(r.isDouble&&r.turkeyA==='turkey_trot')next.doubleTurkeyTrotCount++; if(r.isDouble&&r.turkeyA==='jiblets')next.doubleJibletsCount++; if(r.isThanksgiving)next.doubleWattleWobbleCount++; return next;}),recordGame:bestTurn=>set(s=>({...s,gamesPlayed:s.gamesPlayed+1,highestTurnScore:Math.max(s.highestTurnScore,bestTurn)}))}),{name:'turkey-stats',storage:createJSONStorage(()=>AsyncStorage)}));
