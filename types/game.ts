import { TossScoreResult, TurkeyPosition } from './turkey';
export interface Player { id:string; name:string; score:number; turnsTaken:number; tosses:number; pluckedCount:number; bestTurn:number; bestToss:number }
export type GameStatus='setup'|'playing'|'animating'|'finished';
export interface GameSnapshot { players:Player[]; currentPlayerIndex:number; targetScore:number; turnScore:number; lastToss:TossScoreResult|null; status:GameStatus; totalTurns:number; totalTosses:number; bestTurn:number; bestToss:number; pluckedCount:number; rarePositions:TurkeyPosition[]; winnerId:string|null }
export interface LifetimeStatistics { gamesPlayed:number; totalTosses:number; totalPointsScored:number; pluckedCount:number; pronePoultryCount:number; beakyOneCount:number; gobblerCount:number; turkeyTrotCount:number; jibletsCount:number; wattleWobbleCount:number; doubleGobblerCount:number; doubleTurkeyTrotCount:number; doubleJibletsCount:number; doubleWattleWobbleCount:number; highestTurnScore:number; highestSingleTossScore:number }
